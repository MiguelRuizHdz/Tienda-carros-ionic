import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miguelruizhdz.tiendacarros',
  appName: 'Tienda de Carros',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
